<Window x:Class="LyricSync.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LyricSync.Desktop.Views"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Password:" Grid.Row="2" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,0,0,10"/>
        
        <Button Content="Login" Command="{Binding LoginCommand}" Grid.Row="4" Click="Login_Click"/>
        <TextBlock Text="{Binding Status}" Grid.Row="5" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Window>
